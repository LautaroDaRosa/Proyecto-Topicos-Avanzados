import styled from 'styled-components';

const StUserActionsContainer = styled.div`
  padding-right: 24px;
  display: flex;
  flex-direction: row;
  gap: 12px;
`;

export default StUserActionsContainer;
