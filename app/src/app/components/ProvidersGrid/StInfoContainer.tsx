import styled from 'styled-components';

const StInfoContainer = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;

  button {
    margin-top: 12px;
  }
`;

export default StInfoContainer;
