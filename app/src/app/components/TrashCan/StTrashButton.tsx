import styled from 'styled-components';

const StTrashButton = styled.button`
  position: absolute;
  opacity: 0%;
  width: 48px;
  height: 48px;
  border-radius: 32px;

  :hover {
    cursor: pointer;
  }
`;

export default StTrashButton;
